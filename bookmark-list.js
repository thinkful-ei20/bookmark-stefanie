'use strict';

function generateBookmarkElement() {
  //This is going to generate the HTML to be placed in the DOM
  console.log('generateBookmarkElement works.');
}

generateBookmarkElement();

function render() {
  //This is going to handle rendering the shoppinglist in the DOM
  console.log('render works.');
}

render();

function generateBookmarkListString() {
  //This is going to generate the bookmark list string
  console.log('generateBookmarkListString works.');
}

generateBookmarkListString();

function handleNewAdd() {
  //This function will handle when a user clicks the add button
  console.log('handleNewAdd works.');
}

handleNewAdd();

function handleNewBookmark() {
  //This is going to handle when a user submits a new bookmark
  console.log('handleNewBookmark works.');
}

handleNewBookmark();

function handleDetailedView() {
  console.log('handleDetailedView works.');
  //This is going to handle when a user clicks on bookmark for details
}

handleDetailedView();

function handleClickDelete() {
  console.log('handleClickDelete works.');
  //This function targets the item that the user clicked to be deleted
}

handleClickDelete();

function handleDeletedBookmark() {
  console.log('handleDeletedBookmark works.');
  //This function listens for click, calls delete function and re-renders.

}

handleDeletedBookmark();

function filterRatings() {
  console.log('filterRatings works.');
  //This function listens for click on dropdown, grabs the value of the option selected, renders
}

filterRatings();




